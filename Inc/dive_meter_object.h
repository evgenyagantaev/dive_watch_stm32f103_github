#ifndef DIVE_METER_OBJ_H
#define DIVE_METER_OBJ_H

#include "main.h"

static uint8_t current_depth;
static uint8_t max_depth;
static uint8_t current_dive_time;
static uint8_t old_minutes;

static uint8_t dive_started_flag = 0;

#endif
